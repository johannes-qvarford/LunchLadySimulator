using UnityEngine;
using System.Collections;

public class DetectCol : MonoBehaviour 
{
	/*
	public Vector3 lastPush;
	public bool didPush = false;
	public Transform arm;

	public Vector3 savedPush;

	void OnCollisionEnter(Collision collision)
	{
		savedPush = lastPush;
		arm.inCollision = true;
	}

	void OnCollisionStay(Collision collision)
	{
		/*if(didPush && collision.collider.tag != "Interact")
		{
			rigidbody.AddForce(-lastPush, ForceMode.VelocityChange);
		}
		
	}
	
	void OnCollisionExit(Collision collision)
	{
		arm.inCollision = false;
	}
	*/
}
